<div id="<%= dom_id(insight) %>" class="kpi-card">
  <div class="kpi-icon"><%= insight.icon.presence || "✨" %></div>
  <div class="kpi-value"><%= insight.value %></div>
  <div class="kpi-label"><%= insight.title %></div>
</div>
