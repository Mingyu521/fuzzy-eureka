development:
  adapter: async

test:
  adapter: test

production:
  # A single Render instance (or any single-process deployment) doesn't need
  # Redis for Action Cable pub/sub — async works the same way it does in
  # development. Switch to redis (and add the redis gem) only if this ever
  # runs as more than one process/dyno.
  adapter: async
